# ============================================================================
# SUNA DEFAULT CONFIGURATION
# ============================================================================
# This file contains sensible defaults for local development.
# Copy to .env and customize as needed.
#
# REQUIRED: Only these are needed for basic functionality
# - SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
# - At least one LLM API key (ANTHROPIC_API_KEY, OPENAI_API_KEY, etc.)
#
# OPTIONAL: These enable additional features but aren't required to start
# ============================================================================

# Environment Mode
ENV_MODE=local

##### DATABASE (REQUIRED) #####
# For local Supabase (via npx supabase start):
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_ANON_KEY=<from supabase status output>
# SUPABASE_SERVICE_ROLE_KEY=<from supabase status output>
#
# For cloud Supabase (supabase.com):
# Get these from your Supabase project settings
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

##### REDIS (For local development) #####
# Use "redis" when using docker compose, or "localhost" for fully local
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_SSL=false

##### LLM PROVIDERS (At least one REQUIRED) #####
# Provide at least one API key from these providers:
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=

# AWS Bedrock (optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION_NAME=

# OpenAI-compatible endpoints (optional)
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_API_BASE=

##### OPTIONAL SERVICES #####
# These are NOT required to start Suna. Features using these services
# will be automatically disabled if keys are not provided.

# Search APIs (enables web search functionality)
TAVILY_API_KEY=
RAPID_API_KEY=

# Web scraping (enables page content extraction)
FIRECRAWL_API_KEY=
FIRECRAWL_URL=https://api.firecrawl.dev

# Agent Sandbox (enables isolated code execution with Daytona)
# Without this, agents can still work but won't have sandboxed execution
DAYTONA_API_KEY=
DAYTONA_SERVER_URL=https://app.daytona.io/api
DAYTONA_TARGET=us

##### SECURITY & WEBHOOKS (Recommended for production) #####
MCP_CREDENTIAL_ENCRYPTION_KEY=
TRIGGER_WEBHOOK_SECRET=

##### ADMIN #####
KORTIX_ADMIN_API_KEY=

##### OBSERVABILITY (Optional) #####
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

##### BILLING (Optional) #####
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_DEFAULT_PLAN_ID=
STRIPE_DEFAULT_TRIAL_DAYS=14

##### INTEGRATIONS (Optional) #####
COMPOSIO_API_KEY=
COMPOSIO_WEBHOOK_SECRET=
COMPOSIO_API_BASE=https://backend.composio.dev

# Google OAuth (for Google Slides integration)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/api/google/callback

# Additional service keys
ZENDESK_AUTH_CONFIG=
EXA_API_KEY=
CHUNKR_API_KEY=
